/**
 * One-off backfill: copy Convex `_storage` cover blobs to Cloudflare R2 and update documents.
 * Run manually when R2 is configured (see plan Phase 4). Not executed by CI.
 */
console.log(
  "backfill-r2-migrate: stub — implement listing _storage IDs, R2 put, and Convex patches when migrating.",
);
process.exit(0);
